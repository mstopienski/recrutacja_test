@using DragAndDrop.SampleProject.Model

<div style="width:100%;height:40%;overflow:auto">
    @foreach (var item in EventEntries)
    {
        <EventEntry EventModel="@item" />
    }
</div>

@code {
    [Parameter]
    public List<EventModel> EventEntries { get; set; }
}
